# AURA Admin Web

This folder is reserved for the multilingual admin dashboard implementation.

Recommended bootstrap:

```bash
npx create-next-app@latest admin-web --ts --eslint --tailwind --src-dir
```
